{
  "name": "haq-token-dapp",
  "version": "1.0.0",
  "description": "A DApp for minting and buying HAQ tokens on the Holesky testnet",
  "main": "scripts/deploy.js",
  "scripts": {
    "deploy": "hardhat run --network holesky scripts/deploy.js",
    "test": "hardhat test"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.6",
    "chai": "^4.3.7",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2",
    "hardhat": "^2.24.0"
  }
}
